import {Routes} from "@angular/router";
import {GeneratePage} from "./generate-page/generate-page";

export const generationRoutes: Routes = [
    {
        path: '',
        component: GeneratePage
    }
]
